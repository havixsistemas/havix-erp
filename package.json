{
  "name": "havix-erp",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:api": "npm --workspace @havix/backend run start:dev",
    "dev:web": "npm --workspace @havix/web run dev",
    "dev:mobile": "npm --workspace @havix/mobile run start"
  }
}